[package]
name = "axvisor-mpidr-read"
version = "0.1.0"
edition.workspace = true

[features]
ax-std = ["dep:ax-std", "ax-std/alloc", "axvisor-guestlib/ax-std"]

[dependencies]
ax-std = { workspace = true, optional = true }
axvisor-guestlib = { path = "../../common/guestlib" }
