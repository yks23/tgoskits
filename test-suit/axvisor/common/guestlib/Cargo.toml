[package]
name = "axvisor-guestlib"
version = "0.1.0"
edition.workspace = true
authors = ["OpenAI Codex"]

[features]
ax-std = ["dep:ax-std"]

[dependencies]
ax-std = { workspace = true, optional = true }
